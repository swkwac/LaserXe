# LaserMe Icons

Place LaserMe SVG assets here (e.g. from `Laser me grafiki 14.01.2026`).

Import in components:

```ts
import backIcon from "@/assets/icons/laserme/back.svg";
// or use as img src
```
